#!/bin/bash

export OPENTSDB_VERSION="${opentsdb.version}"
export OPENTSDB_PACKAGE="${opentsdb.package}"

export OPENTSDB_PARCEL=${PARCELS_ROOT}/${PARCEL_DIRNAME}
export OPENTSDB_HOME=${PARCELS_ROOT}/${PARCEL_DIRNAME}/${OPENTSDB_PACKAGE}

